{
  "tasks": {
    "dev": "DENO_ENV=dev deno run --allow-net --allow-env --allow-read --watch server.ts",
    "debug": "deno run --inspect-wait --allow-net server.ts",
    "test": "deno test --allow-net --allow-env --allow-read --no-check"
  }
}
